import { Link } from "react-router-dom";

import { AUTH_PAGE_PATH } from "shared/constants/path";

export const DasboardPage = () => {
  return (
    <div>
      <Link to={AUTH_PAGE_PATH}>gi</Link>
    </div>
  );
};
