export enum THEME_MODE {
  LIGTH = "light",
  DARK = "dark",
}

export interface MyTheme {
  header: {
    background: string;
  };
}
export interface MyThemeOptions {
  header: {
    background: string;
  };
}
